import { Inter, Baloo_2, Chicle, Delius } from "next/font/google";

// const geistSans = Geist({
//   variable: "--font-geist-sans",
//   subsets: ["latin"],
// });

// const geistMono = Geist_Mono({
//   variable: "--font-geist-mono",
//   subsets: ["latin"],
// });

export const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
});

export const baloo_2 = Baloo_2({
  variable: "--font-baloo-2",
  subsets: ["latin"],
});

export const chicle = Chicle({
  variable: "--font-chicle",
  subsets: ["latin"],
  weight: ["400"],
});

export const delius = Delius({
  variable: "--font-delius",
  subsets: ["latin"],
  weight: ["400"],
});
